import { equals, test } from "../../../src";

test('testrunner simple test pass 1', () => {
    equals(1,1);
});

test('testrunner simple test pass 2', () => {
    equals(1,1);
});

test('testrunner simple test pass 3', () => {
    equals(1,1);
});

